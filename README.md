# random-number
random-number

# Random Number Monitor

A tiny script that generates a random number every second  
and alerts you if it passes a defined threshold.

## Usage
```bash
python monitor.py
